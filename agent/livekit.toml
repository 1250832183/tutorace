# LiveKit Agent Configuration
# This file is required for deploying agents to LiveKit Cloud

[agent]
name = "tutorace-tutor"
id = "CA_VGx9kiC4chuE"
entrypoint = "agent.py"

[build]
dockerfile = "Dockerfile"
